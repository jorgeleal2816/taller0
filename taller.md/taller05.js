//Escribe un programa que tome como entrada tus nombres y apellidos completos y cuente c
//uantos caracteres tiene, sin tener en cuenta los espacios.
function contarletras(texto = " "){
    if(typeof texto !== "string"){
        return("el valor ingresado no es uma cadena");
        if(texto == " ")
        return "no a ingresado ningun valor"
    return texto.length
    }
}
console.log(contarletras(hola))